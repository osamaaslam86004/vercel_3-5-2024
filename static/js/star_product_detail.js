
  // Assuming you have some JavaScript variable like userRating
  // that represents the user's rating
  var userRating =0.5;

  // Set the --rating variable in the CSS
  document.documentElement.style.setProperty('--rating', userRating);
